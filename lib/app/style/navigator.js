import { StyleSheet } from 'react-native';
import { h4s, w4s } from '../../styler';

export default StyleSheet.create({
    navigator: {
        flex: 1,
        height: h4s(480),
        width: w4s(320),
    },
});
